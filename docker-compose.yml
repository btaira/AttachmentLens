services:
  attachmentlens:
    build: .
    container_name: attachmentlens
    ports:
      - "5000:5000"
    volumes:
      - ./posts.db:/app/posts.db
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
